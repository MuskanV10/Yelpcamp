<% layout('layout/boiler') %>
    <div id="cluster-map"></div>
    <h1>index</h1>
    <div> <a href="/campground/new">ADD NEW</a></div>

    <% for (let i of c){%>
        <div class="card mb-3">
            <div class="row">
                <div class="col-md-4">
                    <% if(i.image.length) {%>
                        <img crossorigin="anonymous" class="img-fluid" alt="" src="<%=i.image[0].url%>">
                        <% }else {%>
                            <img crossorigin="anonymous" class="img-fluid" alt="" src="">
                            <% } %>
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= i.title %>
                        </h5>
                        <p class="card-text">
                            <%= i.description %>
                        </p>
                        <p class="card-text">
                            <small class="text-muted">
                                <%= i.location%>
                            </small>
                        </p>
                        <a class="btn btn-primary" href="/campground/<%=i._id%>">View <%=i.title%></a>
                    </div>
                </div>
            </div>
        </div>
        <% }%>

            <script>
                const mapToken = '<%-process.env.MAPBOX_TOKEN%>'
                const campgrounds = { features: <%-JSON.stringify(c) %>}
            </script>

            <script src="/js/clusterMap.js"></script>